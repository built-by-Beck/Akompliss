# packages/providers

Concrete provider adapters implementing the interfaces in `packages/contracts`:
`LlamaCppProvider`, `SystemTTSProvider`, `KokoroProvider`, `ElevenLabsProvider`,
`OpenClawAdapter`, etc.

**Placeholder until Phase 2.** Vendor-specific imports are allowed here and nowhere else.
