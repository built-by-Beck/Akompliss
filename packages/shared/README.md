# packages/shared

Cross-cutting utilities shared by services and packages (logging, config parsing, error
types). Keep it small; add code only when at least two packages need it.

**Placeholder until a real need appears.**
